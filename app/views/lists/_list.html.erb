<%= link_to list_path(list),
  class:"card-category link",
  style:"background-image: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.4)), url(#{image_path 'totoro.jpg'})" do %>
  <h2><%= list.name %></h2>
  <div class="d-flex flex-row align-items-center fs-6">
    <i class="fa-solid fa-film me-1"></i>
    <%= pluralize(list.bookmarks.count, "movie") %>
  </div>
<% end %>
